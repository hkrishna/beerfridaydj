<h1>Users</h1>
<ul>
<%- @users.each do |user| %>
<li>
  <span class="user-name"><%= link_to(user.display_name, user_path(user)) %></span>
  <span class="video-count"><%= user.videos.size %></span>
</li>
<%- end %>
</ul>

